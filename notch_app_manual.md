# ガラス基板ノッチ判定ツール 取扱説明書

## 1. はじめに
本書は `notch_app.py` の日常運用手順をまとめた取扱説明書です。  
仕様の詳細は `notch_app_spec.md` を参照してください。

---

## 2. 起動方法
1. Python 実行環境を用意します。
2. ターミナルで以下を実行します。

```bash
python notch_app.py
```

---

## 3. 画面構成
- **左ペイン**
  - 画像ファイル選択
  - 解析パラメータ
  - Basler単発撮像 / 連続Grab
  - PLC 接続・監視操作
- **右ペイン**
  - プレビュー画像（マウスでズーム/パン）
  - ステータス（最新5件）
  - 解析結果（最新5件）

---

## 4. 初期設定（初回のみ推奨）
メニューバー「設定」から各設定を開いて入力してください。

### 4.1 PLC連携
- PLC IP / Port
- トリガ/結果/完了/エラーデバイス
- ソフトウェアトリガON/OFF
- 撮像Timeout[ms]
- 完了パルス幅[ms]
- 自動復旧設定
  - 通信断時にPLC自動再接続
  - 準備完了時に監視自動開始

### 4.2 カメラ設定
- 接続対象のGigEカメラを選択

### 4.3 生存カウンタ
- Dデバイス
- 周期[ms]
- 増分
- PLC接続時の自動開始

### 4.4 一時ファイル管理
- 保持上限
- 対象プレフィックス（例: `BaslerShot_, PLCshot_`）

### 4.5 結果CSV/保存先
- `result.csv` 最大件数
- PLCトリガ撮像画像の保存先（空欄でTemp）

> 設定は自動で `~/.notch_app_config.json` に保存されます。

---

## 5. 手動解析の使い方
1. 「画像選択」で対象画像を読み込みます。
2. 左ペインで解析パラメータ（trim, threshold, bandなど）を調整します。
3. 「プレビュー」で結果を確認します。
4. 必要に応じて「バッチ処理」で複数画像を一括解析します。

### 5.1 解析失敗時
- 解析に失敗した場合、プレビュー枠には**解析前の生画像（RAW）**が表示されます。
- 解析結果欄に `RAW表示` の履歴が追加されます。

---

## 6. カメラ操作
### 6.1 単発撮像
- 「Basler撮像」ボタンで1枚撮像します。

### 6.2 連続Grab
- 「連続Grab開始」でライブ表示
- 「連続Grab停止」で停止

> 連続Grab中は単発撮像できません。

---

## 7. PLC監視運用
1. 「接続」でPLCに接続
2. 必要なら「監視開始」でトリガ監視を開始
3. PLCトリガ入力時に自動で
   - 撮像
   - 解析
   - PLCへ結果返答
   - CSV保存
   を実行します。

### 7.1 返答内容
- RU結果ビット
- RD結果ビット
- Timeout/解析エラービット
- DONEパルス

### 7.2 Dデバイスのビット指定
- `D100.3` のようなドット表記に対応しています（bitは0..15）。

---

## 8. 出力ファイル
- 結果CSV: `result.csv`
- 解析結果画像: `*_result.png`
- PLC撮像画像: `PLCshot_*.png`
- 単発撮像画像: `BaslerShot_*.png`

---

## 9. ログと障害調査
- 実行ログ: `~/.notch_app.log`
- フォルトログ: `~/.notch_app_fault.log`

不具合時は以下を確認してください。
1. ステータス欄（最新エラー）
2. 実行ログ（例外詳細）
3. フォルトログ（フリーズ時のスレッド情報）

---

## 10. よくあるトラブル
### 10.1 PLC通信エラー
- IP/Port、デバイス名表記を確認（例: `M100`, `D100.3`）
- PLC側の許可設定を確認

### 10.2 撮像エラー
- カメラ接続状態、カメラ設定を確認
- 「ソフトウェアトリガで撮像」のON/OFFが実機設定と一致しているか確認
- Timeout値を見直し

### 10.3 解析結果が不安定
- trim比、band値、閾値を調整
- 失敗時はRAW表示画像でエッジ状態を確認

---

## 11. 終了
- メニュー「設定」→「終了」またはウィンドウ右上の閉じるボタンで終了してください。
